pipeline {
    agent {
        
    
            label 'nginx'
    }
   
    
    stages {
        stage('run_container') {
            steps {
                
                sh '''
       if [ -z `docker ps -q --filter="ancestor=nginx:alpine"` ]; then docker run --publish 80:80 --detach nginx:alpine;fi
    ''' 
            }
        }
    }
    
}